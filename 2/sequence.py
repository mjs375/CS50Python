name = "Henry"
#STRING variable

names = ["Harry", "Ron", "Hermione"]
#LISTs use []. Sequence of mutable values.

coordinate = (10.0, 20.0)
#TUPLE: sequence of immutable values. Use ().

#SET: collection of unique* values (unordered)

#DICT: collection of key-value pairs
    # e.g. an actual dictionary. Key: word, value: definition


#Square-bracket notation [] gives access to a specific element [character] of that variable
    # [0]: first item, [1]: second item, &c.

#print(name[0])
print(names[1])
